# Отчёт по ветке `change-domain-name`

Дата проверки: 2026-08-23 (UTC)

## Результат

Локальная ветка `change-domain-name` удалена. Её вершина `657a2c58550a77b49252ab8206060b9ad42dbbe7` уже является предком локальной ветки `main`, поэтому уникальных коммитов и изменений, которые могли бы потеряться при удалении, не было.

Удалённой ветки `origin/change-domain-name` на GitHub не существовало, поэтому удаление на сервере не выполнялось. Текущая ветка после работы — `main`.

## Выполненные шаги

1. Проверено исходное состояние командой `git status --short --branch`.
   - Активной была локальная ветка `change-domain-name`.
   - До начала работы в рабочем дереве уже находились пользовательские изменения: модифицированный `CLAUDE.md` и неотслеживаемый `AGENTS.md`.
2. Проверены локальные и удалённые ссылки командами `git remote -v`, `git branch -a -vv --no-abbrev` и просмотрен граф `git log --graph --decorate --oneline --all`.
   - `change-domain-name` указывала на `657a2c5`.
   - Локальная `main` указывала на `1d500c0`.
   - `origin/main` указывала на `5ac1715`.
3. Найдена база слияния командой `git merge-base main change-domain-name`.
   - Результат: `657a2c58550a77b49252ab8206060b9ad42dbbe7`, то есть сама вершина `change-domain-name`.
4. Проверено направление истории командой `git merge-base --is-ancestor change-domain-name main`.
   - Код завершения `0`: ветка полностью входит в историю `main`.
5. Посчитаны уникальные коммиты командой `git rev-list --left-right --count main...change-domain-name`.
   - Результат `3 0`: у локальной `main` было три последующих коммита, у `change-domain-name` — ноль уникальных.
6. Проверены списки коммитов в обе стороны.
   - `change-domain-name..main`: `d7dae9a`, `0c6ac2a`, `1d500c0`.
   - `main..change-domain-name`: пусто.
7. Проверена разница файлов командами `git diff --stat main...change-domain-name` и `git diff --name-status main...change-domain-name`.
   - Обе команды вернули пустой результат: уникального diff в ветке нет.
8. Выполнена дополнительная patch-проверка `git cherry main change-domain-name`.
   - Результат пустой: отдельных неперенесённых патчей нет.
9. Проверено фактическое состояние GitHub командой `git ls-remote --heads origin main change-domain-name`.
   - Найдена только `refs/heads/main` на `5ac1715`; удалённой `change-domain-name` нет.
10. Перед переключением сравнены версии `CLAUDE.md` в обеих ветках через `git rev-parse <ветка>:CLAUDE.md`.
    - Обе версии имели один blob `43c0a07...`, поэтому перенос существующего изменения рабочего дерева был безопасен.
11. Выполнено переключение `git switch main`.
    - Пользовательские изменения `CLAUDE.md` и `AGENTS.md` сохранились.
12. Выполнено безопасное удаление `git branch -d change-domain-name`.
    - Git подтвердил: `Deleted branch change-domain-name (was 657a2c5)`.
13. Выполнена финальная проверка ссылок и достижимости коммита.
    - `git show-ref --verify refs/heads/change-domain-name` не нашёл локальную ветку.
    - `git branch --contains 657a2c5` показал `main`, поэтому коммит миграции не потерян.
    - `git rev-list --left-right --count origin/main...main` вернул `0 5`: удалённая `main` не содержит пяти локальных коммитов.

## Важное состояние после удаления

Локальная `main` опережает `origin/main` на пять коммитов:

- `f1664ea` — Telegram через tinyproxy;
- `657a2c5` — миграция на шесть доменов (бывшая вершина `change-domain-name`);
- `d7dae9a` — роутеры шести доменов и prod-smoke;
- `0c6ac2a` — HANDOFF по миграции;
- `1d500c0` — снятие старого Traefik-роутера и LE-сертификата.

Эти коммиты не потеряны: они доступны из локальной `main`. Для публикации через pull request следует создать новую ветку от текущей локальной `main`, отправить её в `origin` и открыть PR в `origin/main`. В рамках этой проверки push и создание PR не выполнялись.

## Сохранность рабочей директории

Существовавшие до проверки изменения не редактировались и не удалялись:

- `CLAUDE.md` — modified;
- `AGENTS.md` — untracked.

Этот отчёт создан в конце работы и также является новым неотслеживаемым файлом.
